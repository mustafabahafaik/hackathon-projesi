# packages/sdk

Boş — henüz kurulmadı.

Escrow kontratının client/binding'leri (Soroban CLI'nin ürettiği TypeScript bindings) ve
`apps/web` ile `apps/api` arasında paylaşılan tipler burada yaşayacak. `contracts/escrow`
testnet'e deploy edildikten sonra `stellar contract bindings typescript` ile üretilecek.
